<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>SmartPack</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>0.1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string></string>
	</dict>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>

	<!-- Where the SmartPack backend lives. The simulator shares the Mac's
	     localhost; a real device needs the Mac's LAN address here, plus a
	     matching NSExceptionDomains entry below. -->
	<key>SmartPackAPIBaseURL</key>
	<string>http://localhost:4177</string>

	<!-- Development only: the backend runs over plain HTTP on the local
	     machine. Remove both of these once the API is served over HTTPS. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
		<key>NSExceptionDomains</key>
		<dict>
			<key>localhost</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
			</dict>
		</dict>
	</dict>

	<key>NSCameraUsageDescription</key>
	<string>拍照识别衣物,把它加进你的电子衣橱。Used to photograph clothing so SmartPack can add it to your wardrobe.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>从相册选择衣物照片,加进你的电子衣橱。Used to pick clothing photos to add to your wardrobe.</string>
</dict>
</plist>
