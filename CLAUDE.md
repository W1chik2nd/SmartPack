# CLAUDE.md

本项目的 AI 协作规约统一放在 **[`AGENTS.md`](AGENTS.md)**,那是唯一事实来源。

开工前请先完整阅读 `AGENTS.md`,不要在本文件另写规则(避免两份内容漂移)。

几条最容易踩的,先划重点:

- **薄客户端 + 厚后端**:逻辑全放后端,前端只做展示(详见 AGENTS.md 第 3 节)。
- **不要过度防御**:代码复杂度匹配真实需求,不匹配恐惧(第 4 节)。
- **别乱用哈希/加密**:尤其不要对 OpenAI/LLM 的 prompt、请求、响应做 SHA256 当缓存 key(第 5 节)。
- **改完要验证**:Swift typecheck 命令见第 9 节。
